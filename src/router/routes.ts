export enum ROUTE {
    HOME = "/",
    SEARCH = "search",
    DETAILS = "details/:blogType/:id",
    SIGN_IN = "sign-in",
    SIGN_UP = "sign-up",
    RESET_PASSWORD = "reset-password",
    ACCOUNT = "account",
}